import { Bot } from 'lucide-react';

export default function TeacherAvatar() {
  return (
    <div className="w-10 h-10 rounded-full bg-blue-600 flex items-center justify-center text-white shadow-lg border-2 border-white">
      <Bot size={24} />
    </div>
  );
}
